import { FoyerDirectiveDirective } from './foyer-directive.directive';

describe('FoyerDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new FoyerDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
